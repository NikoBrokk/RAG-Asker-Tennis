APP_ENV=dev
PORT=8501

# --- RAG / indeks ---
KB_DIR=kb
DATA_DIR=data
TOP_K=6

# --- Modus ---
# Sett denne til `true` for å aktivere OpenAI‑basert generering. Når satt
# til `false` bruker applikasjonen kun TF‑IDF og returnerer den første
# setningen fra beste dokument, noe som kan gi ufullstendige eller
# upersonlige svar.
USE_OPENAI=true

# --- OpenAI (kun hvis USE_OPENAI=true) ---
# Legg inn din OpenAI‑nøkkel her for at tjenesten skal kunne kontakte
# språkmodellen. Husk å holde nøkkelen hemmelig og aldri sjekke den inn
# offentlig. Eksempel: sk-xxxx
OPENAI_API_KEY=sk-...

EMBED_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini
